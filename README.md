# Steam Workshop Downloader - Scrollbar In Treeview

本版修复：

- 下载列表滚动条直接挂到 Treeview 内部右边缘
- 不再作为 table 外部独立栏
- 不再单独占一列
- 不会在右侧生成大块空白
- CM/CDN 列表弹窗也同步改为 Treeview 内部滚动条
- 保留 22 号字体、多行显示、滚轮到底部修复

## 运行

```bat
run_windows.bat
```
